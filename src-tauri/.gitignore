# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas

# App icons — the tauri::generate_context! macro reads these at build
# time, so they must be committed (the global "Icon?" ignore matches the
# icons/ dir case-insensitively; re-include it here).
!icons/
